"""Billing and subscription endpoints.

These routes will orchestrate plan management, Stripe handshakes, and entitlement
queries so that the frontend can tailor features per organization.
"""

# router = APIRouter(prefix="/billing", tags=["billing"])
